One hidden cycle, three closure depths:

T^60 = I                             full hidden closure
sigma(n+15) = -sigma(n)              G15 sign depth
iota(n+30) = iota(n)                 G30 identity depth
Lambda(T^n) = chi_n in Z2^2          sheet/frame informative lens

Strong G60 is not merely T^60 = I.
Strong G60 requires priming:
  chi_n must reach (1,1),
and locking:
  chi_n must remain on (1,1) near closure.

Therefore:
  G15 is sign closure,
  G30 is identity closure,
  G60 is full cycle closure,
  primed G60 is full cycle closure made informative and stable.
