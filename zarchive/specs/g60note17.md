Hidden sector start:
  chi0 = (0,0)

Crossings:
  C : (f,h) -> (f+1,h)
  A : (f,h) -> (f,h+1)
  all arithmetic mod 2

Preserving action:
  P : chi unchanged

Retentive sectors:
  (0,0), (1,1)

Primed sector:
  (1,1)

Minimal priming words:
  CA
  AC

Minimal primed-lock words:
  CAPP
  ACPP

Reason:
  - two informative crossings are required to reach (1,1)
  - two preserving positive steps are required to satisfy R >= 2
  - so minimal length is 4
