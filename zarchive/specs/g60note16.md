Minimal lock threshold

Hidden sector:
  chi = (f,h) in Z2^2

Retentive sectors:
  (0,0), (1,1)

Dispersive sectors:
  (1,0), (0,1)

Preserving sign on a residence block:
  sigma(P) = (-1)^(f xor h)

So:
  sigma(P)=+1 on diagonal sectors
  sigma(P)=-1 on off-diagonal sectors

Residence-block reinforcement:
  R_k = sum_{t in B_k} sigma_t

Minimal algebraic threshold:
  Theta = 1

Minimal structural lock threshold:
  Theta_min = 2

Basal lock:
  chi_k = (0,0) and R_k >= 2

Primed lock:
  chi_k = (1,1) and R_k >= 2

Candidate G60 lock:
  enter (1,1) by informative crossing
  then accumulate at least two preserving positive steps before next crossing
