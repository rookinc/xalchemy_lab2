Let Q_t = M_t M_t^T.

A priming event is an admissible perturbation ΔM such that
Q'_0 = (M_0 + ΔM)(M_0 + ΔM)^T = Q_0 + ΔQ
with ΔQ nontrivial in the observable quotient.

The transported pump is locked at scale N only if the observable orbit
Q_t, Q_{t+1}, ...
has period N rather than collapsing to a smaller recurrence class.

However, because Q = MM^T is a quotient lens, it can erase upstairs distinctions.
In particular, if M_{t+k} = -M_t, then Q_{t+k} = Q_t.
So Q may collapse sign-closing to identity-closing.

Therefore:
- Q detects effective informative transport,
- Q detects observable collapse vs observable lock,
- but Q may not fully distinguish G15/G30/G60 unless the missing generator distinctions survive the quadratic projection.
