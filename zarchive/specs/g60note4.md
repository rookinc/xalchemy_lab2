Define the lifted witness by

    W_t := (Q_t, chi_t),   with   Q_t = M_t M_t^T

where chi_t is the smallest transported residual invariant needed to distinguish states that Q identifies.

Then:
- Q captures effective observable action.
- chi captures the hidden closure data lost by the quadratic lens.
- informative action is any admissible perturbation whose induced change in (Q, chi) is nontrivial.
- fallback occurs when Q or (Q, chi) closes too early.
- priming means injecting a transported distinction that prevents premature closure.
- G15/G30/G60 must therefore be stated relative to a chosen witness level:
    Q-level,
    lifted-witness level,
    or full-generator level.
